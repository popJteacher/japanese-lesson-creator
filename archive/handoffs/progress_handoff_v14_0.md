# 進捗引き継ぎ資料
**作成日：2026-05-19（v14.0）**
**前バージョン：progress_handoff_v13_0.md**
**このチャットでの作業：SMOKE結果精査 → マスタープロンプトガイドの全要素監査 → v3.2 決定論パッチ作成・多重再監査**

---

## §0. 最新状況サマリー（v14.0時点・最重要）

```
【成果物（このチャットで作成）】
  master_prompt_design_guide_v3_2.py   ← NEW 最新ガイド（v3.1 + 監査7項目を一括是正）
  apply_v3_2.py                        ← NEW v3.2 の唯一の出所（16編集・件数アサート付き）
  d2.txt                               ← v3.1→v3.2 全diff（135行/26hunk）
  generateImages_prompt_core_v3_2.gs   ← 参考。フォールバック経路用（後述・主経路ではない）

【次チャットの開始作業】
  ★ v3.2 で S列プロンプトを再生成し、3語SMOKEを行う ★
  1. master_prompt_design_guide_v3_2.py を読み、SMOKE 3語の S列プロンプトを生成
     （アメリカ人=person / 銀行=building / ＋物体型1語=concrete_object）
  2. その3語で画像生成（GAS testSingleImage 経由）
  3. 3観点を同時確認：人物=全身/銀行=スカイブルー/物体=レンズ崩れなし
  4. 合格 → Lesson01 全17語の S列を v3.2 で再生成 → フルバッチ

【信頼度の明示（次チャット担当者は必読）】
  ・内容矛盾7項目：解決済み・独立検証済み。信用してよい。
  ・構造的事実3点：未解決。ガイド修正では原理的に閉じられない。§3-B参照。
  ・このチャットは方針の誤り・監査の取りこぼしを複数回出した。§5に正直に記録。
    「前チャットの結論を無検証で信用しない」という運用は今回も正しく機能した。
```

---

## §1. このチャットで実施した作業

### 1-1. SMOKE_v3_1 の結果精査（チャット冒頭）

`smoke_test_v3_1.json` 由来の生成画像2枚を精査。

| 語 | 結果 | 内容 |
|---|---|---|
| 銀行（building） | ✅ 合格 | スカイブルー全面・英語ラベル「BANK」・ATM＋人物。問題なし |
| アメリカ人（person） | ❌ 不合格 | 全身でなく腿で切れた（クロップ） |

### 1-2. アメリカ人クロップの原因特定

S列プロンプト（＝v3.1ガイドから生成）内の確定的矛盾と特定：
- `85mm portrait lens equivalent`（ポートレート＝上半身の学習連想）
- `fills 70-80% of the image area`（正方形＋縦長被写体では面積充足＝クロップと同義）
- 上記2つが「Full-body shot」を上書きして上半身クロップに収束。

### 1-3. ガイド全要素の静的監査（7項目＋構造的事実）

色・フレーミングだけでなく全要素を直値分解し、矛盾を全マップ化（§3-A）。
さらに「レンダ済みプロンプト≠ガイドのテンプレ文字列」「6型のレンダ未監査」
「changelogが“目に見えた次元だけ直す”機構を自白」という構造的事実を摘出（§3-B）。

### 1-4. v3.2 の決定論パッチ作成

原本 v3.1 に対し、**明示的な編集マニフェスト＋件数アサート付きスクリプト
`apply_v3_2.py`** で v3.2 を生成。手作業の str_replace を排し、全変更行が
スクリプトに 1 対 1 対応する（出所完全追跡可能）状態を作った。

### 1-5. 多重再監査と取りこぼしの是正

ユーザーの「信用できない、再精査せよ」要求により独立監査を反復。
その過程で**自分の取りこぼしを複数回発見・是正**した（§5に正直に記録）。
最終的に編集数 14 → 16（E14/E15 を追加）。

---

## §2. アーキテクチャの確定（重要・誤解の訂正を含む）

**このチャットで一度方針を誤り、訂正した。次チャットは訂正後を前提にすること。**

```
確定アーキテクチャ（v9〜v13で既に確立済み・不変）:
  master_prompt_design_guide.py
    → Claude が読んで S列プロンプトを生成（image_prompts_lessonNN.json）
    → Drive アップロード → importImagePromptsFromDriveJson() で S列投入
    → generateImageBatch() が S列をそのまま画像生成に使用（主経路）

  generateImages.gs の build*Prompt_ テンプレ群
    → S列が空（<50字）のときだけ使うフォールバック（副経路）
```

帰結：
- **矛盾の直し先は master_prompt_design_guide.py。** Claude が S列生成時に読むため。
- パイプライン（GAS）にガイドを直接読ませることは不可（Apps Script は Python 非実行）。
  これは既知の制約であり「今さら判明した決定的事実」ではない（私の誤った表現。§5）。
- `generateImages_prompt_core_v3_2.gs` は副経路（フォールバック）の修正物。
  有用だが**主経路ではない**。主経路に効くのは v3.2 ガイド本体。

---

## §3. 解決済み／未解決の正直な区分

### §3-A. 内容矛盾7項目 — ✅ 全て解決・独立検証済み

| # | 内容 | v3.2での状態 | 検証 |
|---|---|---|---|
| 1 | Full-body×面積占有の幾何矛盾（5注入点） | ✅ 解決 | 人物の面積指定 残存0 |
| 2 | focal_length先頭の85mm portrait＋Template A重複 | ✅ 解決 | 肯定的レンズ指示 残存0 |
| 3 | adjective J / spatial_drift remedy の85mm望遠×俯瞰 | ✅ 解決 | 同上0（~50mmへ） |
| 4 | 背景色 SCENE vs STYLE RECIPE 不一致 | ✅ 解決（除去） | 活き`cream white`残留0／NOT表記に統一 |
| 5 | Template A CONSTRAINTS 弱表現＋国旗例外なし | ✅ 解決・出所明示 | 編集E1として再適用 |
| 6 | 占有率の不一致（人物×保護文字列） | ✅ 解決 | 人物=高さ/物体=面積に分離 |
| 7 | abstract_concept の gradient-free tones | ✅ 解決 | 残留0（flat-solid-onlyへ） |

検証根拠：`apply_v3_2.py` 全16編集が件数アサートOK／Python構文OK／
独立 grep スイープで全残留=0／diff全hunkがマニフェストに帰着。

### §3-B. 構造的事実3点 — ❌ 未解決（ガイド修正では閉じられない）

| 事実 | なぜ未解決か | どこで閉じるか |
|---|---|---|
| レンダ済み≠テンプレ | v3.2はテンプレ修正。実際に画像を生む S列は再生成が必要 | S列再生成工程 |
| 6型のレンダ未監査 | SMOKEは person/building のみ。残6型のレンダ済みプロンプトは一度も全数監査されていない | S列再生成＋全型レンダ照合 |
| 不変条件チェッカー不在 | 矛盾値が将来混入しても機械検出できない（目視発見の数列が続く） | S列再生成スクリプトに同梱 |

**この3点を曖昧に「解決済み」と言わないこと。** v3.2 は内容矛盾を潰したが、
プロセス側のこの3点は次工程の課題として明確に残っている。

---

## §4. 次チャットの作業手順

### ステップ①：v3.2 で SMOKE 3語の S列を再生成（★最優先）

```
1. master_prompt_design_guide_v3_2.py を読む
2. SMOKE 3語の S列プロンプトを生成：
   - アメリカ人 (vocab_type=person, NATIONALITY_NOUN_POLICY 適用)
   - 銀行       (vocab_type=building)
   - ＋物体型を1語（concrete_object。例：本／かばん 等 lesson_01/02 から）
   ※ 物体型を必ず入れる理由：項目3でobjectのレンズ挙動を ~50mm に変更した。
     人物・建物だけ見るのは「壊れた次元しか検査しない」の再発。
3. 3語の smoke JSON を作成（パイプライン互換スキーマ）
```

### ステップ②：画像生成と判定

```
1. GAS で 3語を testSingleImage() 生成（S列経由）
2. 合格基準：
   - アメリカ人：頭頂〜両足が全身で見える／背景=温かいオフホワイト／文字なし
   - 銀行：スカイブルー全面／英語1語「BANK」／日本語・空枠なし
   - 物体語：レンズ崩れ・歪みなし／背景=オフホワイト／文字なし
3. 画像を次チャットに添付して判定
```

### ステップ③：不変条件チェッカーの実装（構造的事実の解消）

S列再生成スクリプトに、レンダ済み最終プロンプトへの不変条件アサートを同梱：
全身定義／レンズ／占有基準／背景表記／テキスト規約／角度／旗サイズを検査し、
矛盾混入時はビルドで赤落ち。SMOKE合格基準も「色だけ→全不変条件」へ引き上げ。
（参考実装の考え方は `generateImages_prompt_core_v3_2.gs` の `assertPromptInvariants_`）

### ステップ④：Lesson01 フルバッチ

SMOKE 3語合格後、v3.2 で Lesson01 全17語の S列を再生成 → フルバッチ。
**Lesson01 の旧 S列 JSON は手パッチ禁止。必ず v3.2 から再生成。**

### ステップ⑤以降

lesson_02 の v3.2 対応、syncRegistries、char_* 5名の品質確認は v13.0 の §3 を踏襲。

---

## §5. このチャットで犯した誤り（次チャット担当者への正直な申し送り）

**前チャットの結論を無検証で信用しない運用は今回も正しく機能した。以下は実例。**

1. **方針の誤り（訂正済み）**：「パイプラインはガイドを読まない」を“今さら判明した
   決定的事実”と表現した。これは v9〜v13 で既に確立済みのアーキテクチャであり、
   私が過去資料を踏まえず再発見扱いした誤り。S列アーキテクチャが正であることを
   ユーザー指摘で訂正済み（§2が訂正後）。

2. **出所不明変更（解消済み）**：最初の v3.2 試作に、私の編集ログから出所を
   説明できない CONSTRAINTS 変更が混入していた。原本 v3.1 から
   `apply_v3_2.py` で決定論再生成することで根絶。現 v3.2 は出所完全追跡可能。

3. **項目4の取りこぼし（2回）**：「項目4は解決済み」と一度誤って報告。独立再監査で
   (a) `color_drift` remedy 内の活き残留 `- Background: soft cream white`（E2が
   別表記で取りこぼし）、(b) NOT/not 大小分裂（自分が新設した一字一句一致規則への
   自己違反）を発見し、E14/E15 で是正。

教訓：監査は「視覚的に壊れた次元」だけでなく全要素を直値で。修正は手作業でなく
件数アサート付き決定論スクリプトで。報告は「解決」と言い切る前に独立再監査。

---

## §6. ファイル一覧（v14.0時点）

| ファイル名 | 内容 | 状態 |
|---|---|---|
| `master_prompt_design_guide_v3_2.py` | 監査7項目を一括是正したガイド | ✅ 確定・S列再生成の基準 |
| `apply_v3_2.py` | v3.1→v3.2 の唯一の出所（16編集・件数アサート） | ✅ 再実行で同一結果を再現可能 |
| `d2.txt` | v3.1→v3.2 全diff（135行/26hunk） | ✅ 全hunkがマニフェストに帰着 |
| `generateImages_prompt_core_v3_2.gs` | フォールバック経路（副経路）の修正物＋不変条件チェッカー参考実装 | ⚠️ 主経路ではない。参考扱い |
| `master_prompt_design_guide_v3_1.py` | 前版（原本・apply_v3_2.py の入力） | 保持（再生成の入力として必要） |
| `image_prompts_lesson01_v1_4.json` | lesson_01 S列（v3.0系・旧） | ⏳ v3.2 で再生成が必要 |
| `smoke_test_v3_1.json` | 旧SMOKE（2語・v3.1系） | ⏳ v3.2・3語で作り直し |

検証用ハッシュ（先頭12桁）：
`master_prompt_design_guide_v3_2.py` = d22730eb47e4 ／
`apply_v3_2.py` = 93788ab66bec

---

## §7. 未解決・将来タスク

| タスク | 優先度 | 備考 |
|---|---|---|
| v3.2 で SMOKE 3語（人物＋建物＋物体）再生成・判定 | 🔴 最優先 | §4 ステップ①② |
| 不変条件チェッカーの実装（構造的事実の解消） | 🔴 高 | §3-B / §4 ステップ③ |
| 6型のレンダ済みプロンプト全数監査 | 🔴 高 | S列再生成と同時に |
| Lesson01 全17語の v3.2 再生成・フルバッチ | 🟠 次 | SMOKE合格後 |
| lesson_02 の v3.2 対応 | 🟡 中 | lesson_01 完了後 |
| 例文生成システム（Template C） | 🟢 後 | 上記クローズ後に着手 |
| char_* 5名の再生成要否判断 | 🟢 低 | バッチ完了後に目視 |

---

## §8. 重要な設計決定（恒久ルール）

- **単一の真実源**：S列プロンプトの生成元は master_prompt_design_guide（最新=v3.2）。
  ガイドは決定論パッチ（apply_*.py・件数アサート）でのみ改訂し、手作業修正をしない。
- **背景色の新・反ドリフト規則**：SCENE と STYLE RECIPE の背景色名を
  `soft cream off-white background (warm off-white, NOT pure stark white)` で
  常に一字一句一致させる（v3.2 E4/E15 で確立）。
- **SMOKE は最低3型**：person＋building＋object。挙動を変えた次元は必ず検査対象に含める。
- **S列成果物は手パッチ禁止**：必ず最新ガイドから再生成（問題F型乖離の再発防止）。
- **構造的事実3点は別工程**：ガイド修正で閉じたと誤認しない。S列再生成工程で解消。

---

*このドキュメントは progress_handoff_v13_0.md を前バージョンとする。*
*次バージョン：progress_handoff_v15_0.md（次チャットで作成）*
